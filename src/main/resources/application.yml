server:
  servlet:
    session:
      timeout: 3600
  shutdown: graceful # graceful shutdown ??
  tomcat:
    threads:
      max: 1000 # ??? ? ?? thread? ? ??
      min-spare: 100 # ?? ??? ????(idle) thread? ??
    max-connections: 8192 # ????? connection? ? ??
    accept-count: 200 # ???? ???
    connection-timeout: 300000 # timeout ?? ?? ??
  port: 8080 # ??? ?? ????
  error:
    include-exception: true # ?? ??? exception? ??? ???? ??
    include-stacktrace: always # ?? ??? stacktrace ??? ??? ? ??

spring:
  application:
    name: web-service

  thymeleaf:
    cache: false
    prefix: classpath:views/
    suffix: .html